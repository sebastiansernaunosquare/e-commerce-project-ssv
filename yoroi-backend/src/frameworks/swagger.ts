import swaggerJSDoc from "swagger-jsdoc";

const options: swaggerJSDoc.Options = {
  definition: {
    openapi: "3.0.0",
    info: {
      title: "Yoroi store API Docs",
      version: "1",
    },
    components: {
      securitySchemes: {
        bearerAuth: {
          type: "http",
          scheme: "bearer",
          bearerFormat: "JWT",
        },
      },
    },
    security: [
      {
        bearearAuth: [],
      },
    ],
  },
  apis: ["./src/routes/**/*.ts"],
};

export default swaggerJSDoc(options);
